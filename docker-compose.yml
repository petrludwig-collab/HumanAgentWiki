services:
  db:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_DB: humanagentwiki
      POSTGRES_USER: humanagentwiki
      POSTGRES_PASSWORD: humanagentwiki
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
